import React from 'react'
import Box from '@mui/material/Box'
import PropTypes from 'prop-types'

const Spacer = (props) => {
  return (
    <Box sx={{ height: '100px' }}>
      {/* TODO: Space left intentionally for ads placement */}
    </Box>
  )
}

Spacer.propTypes = {}

export default Spacer
